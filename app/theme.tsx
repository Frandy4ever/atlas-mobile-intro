import React from "react";
import { View } from "react-native";

// This is a placeholder screen that won't actually render
// The theme tab is handled via the tabPress listener in _layout.tsx
const ThemeScreen: React.FC = () => {
  return <View />;
};

export default ThemeScreen;